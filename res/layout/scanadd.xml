<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/back"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".ScanAdd" >

    <TextView
        android:id="@+id/scantext"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="33dp"
        android:gravity="center"
        android:text="@string/scan_hint"
        android:textAppearance="?android:attr/textAppearanceLarge" />

    <EditText
        android:id="@+id/isbn_num"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/scantext"
        android:layout_below="@+id/scantext"
        android:layout_marginTop="24dp"
        android:background="#ffffff"
        android:ems="10"
        android:hint="@string/isbn_hint"
        android:inputType="numberSigned"
        android:singleLine="true" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/ok_button"
        android:layout_width="120sp"
        android:layout_height="40sp"
        android:layout_alignLeft="@+id/isbn_num"
        android:layout_centerVertical="true"
        android:background="@drawable/button_selector"
        android:text="@string/ok" />

    <Button
        android:id="@+id/cancel_button"
        android:layout_width="120sp"
        android:layout_height="40sp"
        android:layout_alignRight="@+id/isbn_num"
        android:layout_centerVertical="true"
        android:background="@drawable/button_selector"
        android:text="@string/cancel" />

</RelativeLayout>